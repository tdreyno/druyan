{
  "private": true,
  "name": "druyan",
  "version": "1.0.0",
  "scripts": {
    "build": "cd packages/druyan && yarn run build && cd ../druyan-react && yarn run build",
    "lint": "cd packages/druyan && yarn run lint && cd ../druyan-react && yarn run lint",
    "test": "cd packages/druyan && yarn run test && cd ../druyan-react && yarn run test"
  },
  "lint-staged": {
    "*.{js,jsx,css,md}": [
      "prettier --write",
      "git add"
    ],
    "*.{ts,tsx}": [
      "tslint --fix",
      "git add"
    ]
  },
  "devDependencies": {
    "husky": "^2.0.0",
    "lint-staged": "^8.1.0",
    "prettier": "^1.15.3",
    "typescript": "^3.2.2"
  },
  "dependencies": {},
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged && yarn test"
    }
  }
}

{
  "private": true,
  "name": "druyan",
  "version": "1.0.0",
  "scripts": {
    "build": "cd packages/druyan && yarn run build && cd ../druyan-react && yarn run build && cd ../druyan-graph && yarn run build",
    "lint": "cd packages/druyan && yarn run lint && cd ../druyan-react && yarn run lint && cd ../druyan-graph && yarn run lint",
    "test": "cd packages/druyan && yarn run test && cd ../druyan-react && yarn run test && cd ../druyan-graph && yarn run test"
  },
  "lint-staged": {
    "*.{ts,tsx,css,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "devDependencies": {
    "husky": "^2.0.0",
    "lint-staged": "^8.1.0",
    "prettier": "^1.15.3"
  },
  "dependencies": {},
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged && yarn test"
    }
  }
}
